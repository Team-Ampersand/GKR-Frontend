import TopBox from '../TopBox'
import * as S from './style'

interface Props {
  id: string | undefined
}

const DetailBox = ({ id }: Props) => {
  return <S.Wrapper></S.Wrapper>
}

export default DetailBox
